[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ledlab"
version = "0.1.0"
description = "My LED Lab Python library"
authors = [
  { name="jim201414", email="jimharisis14@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.7"

dependencies = []
